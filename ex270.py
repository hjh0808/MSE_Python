종목 = []    # 종목을 리스트로 만든다.

삼성 = Stock("삼성전자", "005930", 15.79, 1.33, 2.83)
현대차 = Stock("현대차", "005380", 8.70, 0.35, 4.27)
LG전자 = Stock("LG전자", "066570", 317.34, 0.69, 1.37)

종목.append(삼성)   # 종목에 삼성의 값을 넣는다.
종목.append(현대차)  # 종목에 현대차의 값을 넣는다.
종목.append(LG전자)  # 종목에 LG전자의 값을 넣는다.

for i in 종목:     # 종목을 다 돈다.
	print(i.code, i.per)     # 각 회사의 code와 per의 값을 출력한다.