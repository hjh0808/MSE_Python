def print_max(a, b, c):
	max_val = 0   
	if a > max_val:  # a가 0보다 크다면
		max_val = a    # max_val은 a이다.
	if b > max_val:   # b가 0보다 크다면
		max_val = b   # max_val은 b이다.
	if c > max_val:   # c가 0보다 크다면
		max_val = c    # max_val은 c이다.
	print(max_val)     # max_val을 출력하여라